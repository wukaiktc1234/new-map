<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodtraceability.mapper.ElectronicInvoiceItemMapper">

    <select id="selectByInvoiceId" resultType="com.foodtraceability.entity.ElectronicInvoiceItem">
        SELECT * FROM electronic_invoice_item
        WHERE invoice_id = #{invoiceId}
        AND deleted = 0
        ORDER BY item_no ASC
    </select>

    <delete id="deleteByInvoiceId">
        UPDATE electronic_invoice_item
        SET deleted = 1
        WHERE invoice_id = #{invoiceId}
    </delete>

</mapper>
