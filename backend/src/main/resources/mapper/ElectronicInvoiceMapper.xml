<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodtraceability.mapper.ElectronicInvoiceMapper">

    <select id="selectByVoucherId" resultType="com.foodtraceability.entity.ElectronicInvoice">
        SELECT * FROM electronic_invoice
        WHERE voucher_id = #{voucherId}
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByInvoiceNo" resultType="com.foodtraceability.entity.ElectronicInvoice">
        SELECT * FROM electronic_invoice
        WHERE invoice_code = #{invoiceCode}
        AND invoice_no = #{invoiceNo}
        AND tenant_id = #{tenantId}
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="findByCodeAndNo" resultType="com.foodtraceability.entity.ElectronicInvoice">
        SELECT * FROM electronic_invoice
        WHERE tenant_id = #{tenantId}
        AND invoice_code = #{invoiceCode}
        AND invoice_no = #{invoiceNo}
        AND deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
