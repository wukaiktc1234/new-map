/**
 * 资产管理模块类型定义 - 兼容层
 * 所有类型已迁移至 types/asset.ts，此文件仅做重新导出
 */
export type {
  AssetStatistics,
  SyncSettings,
  SyncLog,
  DisposalRecord as DisposalItem,
  DisposalCreateDTO as DisposalFormData,
} from './asset'
