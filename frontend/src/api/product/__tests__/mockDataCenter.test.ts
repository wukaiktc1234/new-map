/**
 * 统一 Mock 数据中心 - 测试套件
 *
 * 注意：原 mockDataCenter.ts 已在 mock 数据清理中删除。
 * 本测试文件保留为占位，待真实 API 落地后替换为真实接口测试。
 */

import { describe, it, expect } from 'vitest'

describe('mockDataCenter (已清理)', () => {
  it('placeholder - mock 数据已删除，待真实 API 落地后补充测试', () => {
    expect(true).toBe(true)
  })
})
