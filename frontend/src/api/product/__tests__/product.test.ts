/**
 * 产品管理模块自动化测试套件
 *
 * 注意：原 mockData.ts 已在 mock 数据清理中删除。
 * 本测试文件保留为占位，待真实 API 落地后替换为真实接口测试。
 */

import { describe, it, expect } from 'vitest'

describe('product mock API (已清理)', () => {
  it('placeholder - mock 数据已删除，待真实 API 落地后补充测试', () => {
    expect(true).toBe(true)
  })
})
